apiVersion: v1
kind: Service
metadata:
  labels:
    service: frontend-feed
  name: frontend-feed
spec:
  ports:
  - name: "8100"
    port: 8100
    targetPort: 80
  selector:
    service: frontend-feed