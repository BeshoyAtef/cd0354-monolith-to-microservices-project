apiVersion: v1
kind: Service
metadata:
  labels:
    service: udgram_frontend
  name: udgram_frontend
spec:
  ports:
  - name: "8100"
    port: 8100
    targetPort: 80
  selector:
    service: udgram_frontend